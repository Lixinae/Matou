On doit faire une RFC sous forme pdf a rendre

C'est quoi RFC ?


Les RFC d�taillent le protocole.
la RFC se dois de pr�ciser la syntaxe et la grammaire du protocole, les d�tails d'impl�mentation �tant sp�cifi� sous forme de conseils (parfois appuy�s)
n'importe quel codeur peux en suivant � la lettre une RFC, r�aliser une application impl�mentant le protocole.

En bref on explique comment �a marche


Serveur : 
Boucle
Socket = recu(bb)
If bb!=null { 
If new pseudo : rentre (pseudo,socket) dans la map
If message all: envoie message � tous (sauf socket)
If list client : renvoie la list client(� socket)
}


matou.client.Client :


Rentrer pseudo.
Envoie pseudo serveur
Boucle
Demande que veux tu faire?
scan(line)a recu||wait(0.1second)
If line!=null{
if-List client  : /getList
if-Message client : /w nomduclient
if-Message all : /all
Else mauvais format
}
test reception:
If packet recu!=null:
If message autre client :Thread lire le message break;
If confirmation client : connection client break;
If list client : thread affichage liste break;
Else mauvais format